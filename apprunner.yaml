version: 1.0
runtime: nodejs18

build:
  commands:
    build: 
      - npm ci && npm run build

run:
  command: node backend/index.js
  network:
    port: 8080
    env:
      - name: APP_PORT
        value: "8080"
