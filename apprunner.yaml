version: 1.0
runtime: nodejs18

build:
  commands:
    build: 
      - npm ci && npm run build

run:
  command: node backend/index.js
  network:
    port: 8080

health:
  path: /health
  interval: 10
  timeout: 5
  healthy_threshold: 3
  unhealthy_threshold: 3